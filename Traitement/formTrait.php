<?php
/*echo"bonjour";

var_dump($_POST['message']);
$nom = $_POST['nom'];
$mail = $_POST['email'];
$text= $_POST['message'];
$my_email = 'sebastiencoffigniez@gmail.com';
$errors = "";
var_dump($my_email);
var_dump($mail);
var_dump($text);
var_dump($nom);
if(empty($nom)  ||
   empty($_POST['email']) ||
   empty($_POST['message']))
{
    $errors .= "\n Erreur, tous les champs sont requis";
}

    if( empty($errors))
    {
    $to = $my_email;
    $email_subject = "Contact via formulaire de : $nom";
    $email_body = "Vous avez reçu un nouveau message. ".
    " Voici les détails::\n Name: $nom \n ".
    "Email: $mail\n Message \n $text";
    $headers = "From: $my_email\n";
    $headers .= "Reply-To: $mail";
    mail($to,$email_subject,$email_body,$headers);
    var_dump($email_body);
    //redirect to the 'thank you' page
   // header('Location: Contact_reussi.html');
    }

    */
?>
